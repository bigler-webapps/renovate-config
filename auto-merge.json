{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Opt-in preset: enables auto-merge for low-risk updates. Renovate watches CI on the PR itself and merges only when checks pass — no branch protection required.",
  "lockFileMaintenance": {
    "enabled": true,
    "automerge": true,
    "schedule": ["before 6am on monday"]
  },
  "packageRules": [
    {
      "description": "Auto-merge patch, pin, digest and lockfile updates",
      "matchUpdateTypes": ["patch", "pin", "digest", "lockFileMaintenance"],
      "automerge": true
    },
    {
      "description": "Auto-merge minor and patch dev dependency updates",
      "matchDepTypes": ["devDependencies"],
      "matchUpdateTypes": ["minor", "patch"],
      "automerge": true
    }
  ]
}
