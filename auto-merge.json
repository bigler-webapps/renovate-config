{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Opt-in preset: auto-merge all non-major updates after green CI. Majors stay in the Dependency Dashboard for manual approval.",
  "packageRules": [
    {
      "description": "Auto-merge all non-major updates",
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
      "automerge": true
    }
  ]
}
